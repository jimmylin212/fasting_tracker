
"use client"; 
// This file ensures the root path '/' uses the content from (main)/page.tsx, 
// so it can inherit the (main)/layout.tsx which includes the AppSidebar.

import HomePageWithSidebar from './(main)/page';

export default HomePageWithSidebar;
